/*
- What's is string?
- Declsrstion & initialization of string
- printing a string
*/
#include<stdio.h>
int main()
{
    // *** Basic =
    /*
    char s1[3] = {'a','b','\0'};

    //char s1[] = " Tirtha \
                           Babu";
    printf("s1 = %s\n",s1);


                        //  input and display string

      char s1[30];
      printf("Enter your full name :");
      //scanf speace ar porar word pint krta na paray gets use krci
      gets(s1);

      printf("Full name = %s\n",s1);

                              // display string chracter wise

    char s1[] = "Tirtha";
    int i =0;
    while(s1[i] != '\0')
    {
        printf("%c\n",s1[i]);
        i++;
    }

                      //finding length of string using strlen() function

     char s1[] = "Tirtha babu sarkar";

     int len = strlen(s1);
     printf("Lenght = %d\n",len);

                            //findinf length of without using strlen() function

      char s1[] = "Tirtha babu sarkar";
      int i=0, len =0;
      while(s1[i] != '\0')
      {
          i++;
          len++;
      }
      printf("Lenght = %d\n",len);



                                String - copy string using strcpy()


     char source[] = "C programming";
     char target [20];

     strcpy(target, source);
     printf("Source string =%s\n",source);
     printf("Target string =%s\n",target);


}
*/
